// TODO: - Create a constructor to create Record objects with artist, title, price
var Record = function(artist, album, price) {
  this.artist = artist;
  this.album = album;
  this.price = price;
};

module.exports = Record;
